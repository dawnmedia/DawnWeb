---
import Icon from "@components/ui/icons/Icon.astro";
const { title, url } = Astro.props;

interface Props {
  title?: string;
  url?: string;
}

const baseClasses =
  "group inline-flex items-center justify-center gap-x-3 rounded-full px-4 py-3 text-center text-sm font-medium text-neutral-700 ring-zinc-500 focus-visible:ring transition duration-300 outline-none";
const borderClasses = "border border-transparent";
const bgColorClasses = "bg-blue-500 dark:focus:outline-none";
const hoverClasses = "hover:shadow-2xl hover:shadow-blue-300 hover:bg-blue-600 dark:hover:bg-blue-600 dark:hover:shadow-blue-600";
const fontSizeClasses = "2xl:text-base text-white dark:text-white hover:text-white dark:hover:text-white";
const ringClasses = "dark:ring-zinc-200";
---

<a
  class={`${baseClasses} ${borderClasses} ${bgColorClasses} ${hoverClasses} ${fontSizeClasses} ${ringClasses}`}
  href={url}
>
  {title}
</a>
